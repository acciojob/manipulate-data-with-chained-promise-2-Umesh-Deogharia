//your JS code here. If required.

let arr = [1,2,3,4];

let promise = new promise(resolve , reject)=>{
	setTImeout(()=>{
	let oddNumbers = arr.filter((age)=>{
		return age % 2 !== 0;
	});
	},3000);

	setTimeout(()=>{
		
	})
		
	
}